<Window x:Class="EmployeeManagement.Desktop.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:constants="clr-namespace:EmployeeManagement.Desktop.Constants"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="720" Width="1280" >
    <DockPanel>
        <ContentControl DockPanel.Dock="Top" prism:RegionManager.RegionName="{x:Static constants:Regions.TopPanelRegion}"/>
        <TabControl DockPanel.Dock="Top">
            <TabItem Header="Strona główna">
                <ContentControl prism:RegionManager.RegionName="{x:Static constants:Regions.MainRegion}"/>
            </TabItem>
            <TabItem Header="Pracownicy">
                <ContentControl prism:RegionManager.RegionName="{x:Static constants:Regions.EmployeesRegion}"/>
            </TabItem>
            <TabItem Header="Obowiązki">
                <ContentControl prism:RegionManager.RegionName="{x:Static constants:Regions.DutiesRegion}"/>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
